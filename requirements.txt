# Runtime dependencies for NWM.
# For development, benchmarking, and plotting install the extras instead:
#   pip install -e ".[dev,benchmark]"
# This file mirrors the runtime `dependencies` in pyproject.toml.
numpy>=1.20.0
gymnasium>=0.26.0
