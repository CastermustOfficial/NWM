"""Reproducible benchmark suite comparing NWM against RL baselines.

Run with:

    python -m benchmarks.run_benchmark --quick      # fast smoke run
    python -m benchmarks.run_benchmark              # full protocol

Results (per-run JSON, aggregated CSV, and plots) are written under ``results/``.
"""
